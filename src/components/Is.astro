---
const { header } = Astro.props;
const { data } = Astro.props;
---

<a href={`is/${data.slug}`}>
  <article>
    <h2>{data.name}</h2>

    <!-- <img class="is_img" src="img/mandarin.webp" alt="billede af is" />  -->
    <img src={"/img/varianter" + data.image} alt="is" />
  </article>
</a>
<slot />

<style>
  .is_img {
    border-radius: 50%;
    height: 250px;
    width: 250px;
  }
</style>
